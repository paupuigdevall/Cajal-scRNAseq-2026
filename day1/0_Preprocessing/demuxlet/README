## To run demuxlet as an arrayed job:

## Note the VCF is being read from another folder, and had to be slightly modified respect to the Zenodo file (ie. reannotation of chromosomes from "1" to "chr1")

sbatch --array=1-3 run_demuxlet.sh paramFiles.txt
